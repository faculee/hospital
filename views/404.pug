extends layout

block content
  .card.shadow.mt-5
    .card-body.text-center
      h1.text-danger 404 - Página no encontrada
      p La ruta "#{url}" no existe en el sistema.
      a.btn.btn-primary.mt-3(href="/") Volver al inicio
